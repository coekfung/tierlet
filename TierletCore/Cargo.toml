[package]
name = "tierlet-core"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]

[features]
bindgen = ["uniffi/cli"]

[dependencies]
uniffi = "0.32.0"

[[bin]]
name = "uniffi-bindgen"
path = "uniffi-bindgen.rs"
required-features = ["bindgen"]
